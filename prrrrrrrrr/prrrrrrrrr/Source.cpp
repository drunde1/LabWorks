#include <string>
#include <iostream>
#include <vector>
using namespace std;



int main()
{
    
	/*int fault;
	 srand(time(NULL));
	 int massx[31] = { 50, 100, 150, 200 ,250 ,300 ,350, 400 ,450 , 500 ,550, 600, 650, 700, 750, 800,
	 850, 900, 950, 1000, 1050, 1100,1150 ,1200, 1250, 1300, 1350, 1400, 1450, 1500 };
	 int x = rand() % 29 + 1;
	 int massy[13] = { 50, 100, 150, 200 ,250 ,300 ,350, 400 ,450 , 500 ,550, 600 };
	 int y = rand() % 11 + 1;
	 int x1, x2, y1, y2;
	 fault = rand() % 26;
	 if (rand() % 2 == 0)
		 fault = -fault;
	 x2 = massx[x] + fault;
	 y2 = massy[y] + fault;
	 x1 = massx[rand() % x] + fault;
	 y1 = massy[rand() % y] + fault;
	 float length = (float)sqrt(abs(y2 - y1) * abs(y2 - y1) + abs(x2 - x1) * abs(x2 - x1));
	 float angle = (float)atan(abs(y2 - y1) / abs(x2 - x1)) * 180 / M_PI;
	 int Y = 0;
	 line = (Line*)realloc(line, nl * sizeof(Line));
	 nl += 1;
	 if (rand() % 2 == 0)
	 {
		 Y = y2;
		 angle = -angle;
	 }
	 else
	 {
		 Y = y1;
	 }*/

}